---
title: "API"
toc: true
toc-depth: 4
---

```{julia}
#| echo: false
#| output: false
using RapidRefreshData
```

```{julia}
#| echo: false
#| output: asis
export_names = filter(!=(:RapidRefreshData), names(RapidRefreshData))

classify(b) = b isa Type ? :type : b isa Function ? :function : :constant

for (kind, label) in [(:type, "Types"), (:function, "Functions"), (:constant, "Constants")]
    items = filter(n -> classify(getfield(RapidRefreshData, n)) == kind, export_names)
    isempty(items) && continue
    println("## $label
")
    for name in items
        binding = getfield(RapidRefreshData, name)
        doc = string(Docs.doc(binding))
        println("""
        ::: {.card .mb-3}
        ::: {.card-header .api-item-header}
        #### `$name` {#$(lowercase(string(name)))}
        :::
        ::: {.card-body}
        $doc
        :::
        :::
        """)
    end
end
```
